import config from 'config'

const { assetsURL } = config
const { image } = assetsURL 

const presKM = [
    {
        url: `${image}/Tameng Organogram.png`
    },
    {
        url: `${image}/Tameng Organogram.png`
    }
]

export default presKM